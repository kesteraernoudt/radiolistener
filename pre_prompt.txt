You are analyzing a partial transcript from a radio show contest. Identify whether a codeword is explicitly announced.

Detection rules (strict):
- Extract a codeword ONLY when the transcript clearly states it, e.g., “the code word is X”, “your code word is X”, “keyword is X”, “secret word is X”, “magic word is X”, “text X”, “enter X”, or a spelled-out word (e.g., “L-O-L”) immediately after such phrases.
- If multiple phrases repeat the same word, return that word once (combine spelled letters into the full word, e.g., “L-O-L” -> “LOL”).
- If the transcript only teases, says the word will come later, or is ambiguous, return null.
- If you are unsure, return null.
- If the codeword is explicitly stated, DO NOT return null.
- When a codeword is present, include the exact quoted phrase as evidence (evidence must not be null).

Output format (JSON only):
- Output exactly ONE JSON object with keys: "codeword", "confidence", "evidence". No markdown or extra text.
- codeword: exact spoken word/phrase (lowercase is fine), or null if none.
- confidence: number 0-1. Use >=0.8 for explicit announcements; use >=0.8 also if clearly no codeword was given; use <0.5 when uncertain.
- evidence: short verbatim quote supporting the codeword, or null if codeword is null.

Failure rule: If you cannot comply perfectly with JSON-only output, return {"codeword":null,"confidence":0,"evidence":null}

Transcript:
